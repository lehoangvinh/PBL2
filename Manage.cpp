#include "Manage.h"
#include <iostream>
Manage::Manage(/* args */)
{
}

Manage::~Manage()
{
}
// ostream &operator<<(ostream &cout, Computer & cp)
// {
//   Document docStudent,docComputer;
//   int index;
//   Database dbStudent(PATH_DB_STUDENT, docStudent); // lấy database
//   Database dbComputer(PATH_DB_COMPUTER, docComputer);
//   string s =dbStudent.Read(index, "name", docStudent);// đọc database, số index, key "name", trả về string
//   return cout<<"Name: "<<s<< endl;
// }
void Manage::ShowComp()
{
  // Document docStudent, docComputer;
  // Database dbStudent(PATH_DB_STUDENT, docStudent); // lấy database
  // Database dbComputer(PATH_DB_COMPUTER, docComputer);
  // string s =dbComputer.Read(1, "id", docComputer);// đọc database, số index, key "id", trả về string
  // cout<<s;
  // s =dbComputer.Read(1, "name", docComputer);// đọc database, số index, key "name", trả về string
  // cout<<s;
  // s =dbComputer.Read(1, "timeUsed", docComputer);// đọc database, số index, key "name", trả về string
  // cout<<s;
  // s =dbComputer.Read(1, "beingUsedByStudent", docComputer);// đọc database, số index, key "name", trả về string
  // cout<<s<<endl;
  // // char id[] = "102210140";
  // char name[] = "Vinh";
  // char idComputer[] = "2";
  // dbStudent.Create(id, name, idComputer, PATH_DB_STUDENT, docStudent);
  // cout << dbStudent.Read(2, "name", docStudent); // đọc database, số 2, key "name", trả về string
  // dbStudent.Update(5, name, idComputer, PATH_DB_STUDENT, docStudent);
  // dbStudent.Delete(9, PATH_DB_STUDENT, docStudent);
}